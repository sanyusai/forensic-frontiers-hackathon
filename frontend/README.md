# PhishGuard frontend

The interface is implemented in `app/page.tsx` and `app/globals.css`. From this
folder, run `npm install` followed by `npm run dev`. The browser connects to the
local FastAPI service at `http://127.0.0.1:8000`.

See the project-level `README.md` for the complete setup and demonstration
instructions.
