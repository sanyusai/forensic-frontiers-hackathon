# Model directory

Place the trained classifier here as:

`phishing_model.joblib`

You can copy the working model from your current Phishguard-gmail project or
create a new one with `train_model.py`. The API still starts without a model,
but the machine-learning check will report that it is unavailable.
